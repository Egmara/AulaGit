Adicionar alteracoes (no master por enquanto)

git add nome_do_arquivo
git commit -m "mensagem sobre a mudanca"
git push

Comando para criar o Branch e trabalhar no Branch
git checkout -b Nome_Branch

Para ver os Branchs
git branch

//fica o seu

git checkout master // volta os arquivos para o estado master

